# [修正] 初期生成の不要なコメントアウトを削除して整理 (v.1.2.0+9)
name: hgl_order_sheet
description: "HGLオーダーシート — LOCO TRIBE用ダーツリーグ・オーダーシート管理アプリ（LIFF + Flutter Web + Firebase）"
publish_to: 'none'
version: 1.9.1+22

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.14.0
  cloud_firestore: ^6.9.0
  provider: ^6.1.5+1
  crypto: ^3.0.7
  intl: ^0.20.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image_f6d8df.png